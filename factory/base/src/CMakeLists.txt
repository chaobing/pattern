SET(LAYER_BASE_LIB layer_base)

AUX_SOURCE_DIRECTORY(. LAYER_BASE_SRC)
AUX_SOURCE_DIRECTORY(layer LAYER_SRC)

SET(LIBRARY_OUTPUT_PATH ${LIB_PATH})
ADD_LIBRARY(${LAYER_BASE_LIB} SHARED ${LAYER_BASE_SRC} ${LAYER_SRC})

